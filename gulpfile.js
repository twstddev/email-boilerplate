var gulp = require( "gulp" );
var plugins = require( "gulp-load-plugins" )();
// load tasks from the gulp directory
require( "gulp-load" )( gulp )( "gulp" );

gulp.task( "development", function() {
} );

gulp.task( "default", function() {
} );
